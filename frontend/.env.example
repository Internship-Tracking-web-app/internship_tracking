# Frontend environment variables for Vite
# Set VITE_API_BASE_URL to your deployed backend (Render) URL.
# When deploying to Vercel, set this variable in the Vercel dashboard instead of committing a .env file.

VITE_API_BASE_URL=https://internship-tracking-8.onrender.com
